<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sea Chef Labs - Pro Dashboard</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            color: #fff;
            min-height: 100vh;
            padding: 20px;
        }
        .container { max-width: 1200px; margin: 0 auto; }
        
        /* Header & Wallet */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }
        h1 {
            font-size: 1.8rem;
            background: linear-gradient(135deg, #00d9ff, #00ff88);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .connect-btn {
            background: rgba(255,255,255,0.1);
            border: 1px solid #00d9ff;
            color: #00d9ff;
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }
        .connect-btn:hover { background: #00d9ff; color: #0f172a; }
        .connect-btn.connected { background: #00ff88; color: #0f172a; border-color: #00ff88; }

        /* Stats Row */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        .stat-card {
            background: rgba(255,255,255,0.05);
            border-radius: 12px;
            padding: 20px;
            border: 1px solid rgba(255,255,255,0.05);
        }
        .stat-label { color: #8892b0; font-size: 0.9rem; margin-bottom: 10px; }
        .stat-value { font-size: 2rem; font-weight: bold; }
        .stat-change { font-size: 1rem; margin-top: 5px; }
        .positive { color: #00ff88; }
        .negative { color: #ff4757; }

        /* Section Titles */
        .section-title {
            font-size: 1.3rem;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        /* Researched Coins Grid */
        .researched-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 15px;
            margin-bottom: 30px;
        }
        .coin-card {
            background: rgba(0, 217, 255, 0.05);
            border: 1px solid rgba(0, 217, 255, 0.2);
            border-radius: 12px;
            padding: 20px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }
        .coin-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px; }
        .coin-symbol { font-weight: bold; font-size: 1.2rem; }
        .coin-price { color: #8892b0; font-size: 0.9rem; }
        .coin-reason { color: #cbd5e1; font-size: 0.85rem; margin-bottom: 15px; flex-grow: 1; }
        .buy-btn {
            background: linear-gradient(135deg, #00d9ff, #00ff88);
            color: #0f172a;
            border: none;
            padding: 10px;
            border-radius: 6px;
            font-weight: bold;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            display: block;
        }
        .buy-btn:hover { opacity: 0.9; }

        /* Holdings List */
        .holdings-list {
            background: rgba(255,255,255,0.03);
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 30px;
        }
        .holding-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 1px solid rgba(255,255,255,0.05);
        }
        .holding-item:last-child { border-bottom: none; }
        .holding-info { display: flex; align-items: center; gap: 15px; }
        .token-icon {
            width: 35px; height: 35px; border-radius: 50%;
            background: rgba(255,255,255,0.1);
            display: flex; align-items: center; justify-content: center;
            font-weight: bold; font-size: 0.8rem;
        }

        /* AI Agents */
        .agents-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 15px;
        }
        .agent-card {
            background: rgba(255,255,255,0.05);
            padding: 15px;
            border-radius: 10px;
            border-left: 4px solid #00ff88;
        }
        .agent-name { font-weight: 600; margin-bottom: 5px; }
        .agent-status { color: #00ff88; font-size: 0.85rem; }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>🌊 Sea Chef Labs</h1>
            <button class="connect-btn" id="connectBtn" onclick="connectWallet()">Connect Wallet</button>
        </header>

        <!-- Top Stats Windows -->
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-label">Total Portfolio Value</div>
                <div class="stat-value" id="totalValue">$17,355.00</div>
                <div class="stat-change positive" id="portfolioChange">+3.85% (+$645.50)</div>
            </div>
            <div class="stat-card">
                <div class="stat-label">24h Profit/Loss</div>
                <div class="stat-value positive">+$645.50</div>
                <div class="stat-change">Across all chains</div>
            </div>
            <div class="stat-card">
                <div class="stat-label">Cash Balance</div>
                <div class="stat-value">$2,100.00</div>
                <div class="stat-change">Ready to deploy</div>
            </div>
        </div>

        <!-- RESEARCHED COINS (Moved to Top) -->
        <div class="section-title">🎯 Researched Coins to Buy</div>
        <div class="researched-grid">
            <div class="coin-card">
                <div class="coin-header">
                    <span class="coin-symbol">PEPE</span>
                    <span class="coin-price">$0.000012</span>
                </div>
                <div class="coin-reason">Whale accumulation detected. AI sentiment score: 92/100.</div>
                <a href="https://app.uniswap.org/#/swap?outputCurrency=0x6982508145454Ce325dDbE47a25d4ec3d2311933" target="_blank" class="buy-btn">Buy on Uniswap</a>
            </div>
            <div class="coin-card">
                <div class="coin-header">
                    <span class="coin-symbol">WIF</span>
                    <span class="coin-price">$2.45</span>
                </div>
                <div class="coin-reason">Breaking out of consolidation. TG bot volume spike.</div>
                <a href="https://jup.ag/swap/SOL-So11111111111111111111111111111111111111112" target="_blank" class="buy-btn">Buy on Jupiter</a>
            </div>
            <div class="coin-card">
                <div class="coin-header">
                    <span class="coin-symbol">RNDR</span>
                    <span class="coin-price">$7.80</span>
                </div>
                <div class="coin-reason">AI narrative trending. Strong support level holding.</div>
                <a href="https://app.uniswap.org/#/swap?outputCurrency=0x6de037ef9ad2725eb40118bb1702ebb27e4aeb24" target="_blank" class="buy-btn">Buy on Uniswap</a>
            </div>
        </div>

        <!-- Current Holdings -->
        <div class="section-title">🪙 Your Token Holdings</div>
        <div class="holdings-list">
            <div class="holding-item">
                <div class="holding-info">
                    <div class="token-icon">ETH</div>
                    <div>
                        <div style="font-weight:600">Ethereum</div>
                        <div style="color:#8892b0; font-size:0.9rem">2.45 ETH</div>
                    </div>
                </div>
                <div style="text-align:right">
                    <div style="font-weight:600">$10,106.25</div>
                    <div class="positive">+5.2%</div>
                </div>
            </div>
            <div class="holding-item">
                <div class="holding-info">
                    <div class="token-icon">SOL</div>
                    <div>
                        <div style="font-weight:600">Solana</div>
                        <div style="color:#8892b0; font-size:0.9rem">45.2 SOL</div>
                    </div>
                </div>
                <div style="text-align:right">
                    <div style="font-weight:600">$6,780.00</div>
                    <div class="negative">-1.5%</div>
                </div>
            </div>
        </div>

        <!-- AI Agents -->
        <div class="section-title">🤖 Whale AI Agents & TG Bots</div>
        <div class="agents-grid">
            <div class="agent-card">
                <div class="agent-name">Whale Alert Bot</div>
                <div class="agent-status">● Active - Tracking $1M+ moves</div>
            </div>
            <div class="agent-card">
                <div class="agent-name">AI Market Analyzer</div>
                <div class="agent-status">● Active - Scanning 50+ pairs</div>
            </div>
            <div class="agent-card">
                <div class="agent-name">Sniper Bot Tracker</div>
                <div class="agent-status">● Active - 3 new tokens detected</div>
            </div>
            <div class="agent-card">
                <div class="agent-name">TG Signal Groups</div>
                <div class="agent-status">● Active - 12 groups connected</div>
            </div>
        </div>
    </div>

    <script>
        // REAL WALLET CONNECTION LOGIC
        async function connectWallet() {
            const btn = document.getElementById('connectBtn');
            
            // Check if MetaMask is installed
            if (typeof window.ethereum !== 'undefined') {
                try {
                    // Request account access
                    const accounts = await window.ethereum.request({ method: 'eth_requestAccounts' });
                    const account = accounts[0];
                    
                    // Update button to show connected address
                    const shortAddress = account.substring(0, 6) + '...' + account.substring(38);
                    btn.innerText = shortAddress;
                    btn.classList.add('connected');
                    
                    // Listen for account changes
                    window.ethereum.on('accountsChanged', function (accounts) {
                        if (accounts.length === 0) {
                            btn.innerText = 'Connect Wallet';
                            btn.classList.remove('connected');
                        } else {
                            const newAddr = accounts[0].substring(0, 6) + '...' + accounts[0].substring(38);
                            btn.innerText = newAddr;
                        }
                    });
                } catch (error) {
                    console.error("User rejected connection");
                }
            } else {
                alert("MetaMask is not installed! Please install it to connect your wallet.");
                window.open('https://metamask.io/download/', '_blank');
            }
        }

        // Simulate live price ticking
        setInterval(() => {
            const totalVal = document.getElementById('totalValue');
            let current = parseFloat(totalVal.innerText.replace(/[$,]/g, ''));
            let change = (Math.random() - 0.5) * 50;
            let newVal = current + change;
            totalVal.innerText = '$' + newVal.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2});
        }, 3000);
    </script>
</body>
</html>
